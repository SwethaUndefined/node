// const { addNum, subNum, mulNum, divNum } = require('./calculator.js');
//Instead of above line we can just use import like import {addNum....} from "./calculator"
import {addNum,subNum,mulNum,divNum} from './calculator.js';

console.log(addNum(2,3));
console.log(subNum(2,3));
console.log(mulNum(2,3));
console.log(divNum(2,3));